Review complete — no implementation yet, per request. Proposed roadmap (items independently approvable):

**P1 — Render/bake path (dominant measured cost)**
1. Narrow the bake cache key (bake_and_rank.py:22594-22621) to only the code actually executed in the bake + post-bake constraining path (drop upstream-only modules like structural_refinement.py whose effect is already captured in the cleaned-motion JSON / preview HTML). Verify by tracing the call graph of `_bake_preview_loops_with_playwright_uncached` + `constrain_baked_payload_to_source_articulation`, add a focused regression test that a change to an upstream-only module does NOT invalidate the bake cache while a render-path change does.
2. Fold the up-to-3× per-loop bake (initial → camera rebake → orientation rebake, bake_and_rank.py:22779-22795) into a probe-export + single final bake, since both decisions are deterministic functions of the export payload. Add rebake-rate counters to manifests and confirm savings on the two-exercise test base.
3. Make selected-section materialization content-keyed (like stage_cache) instead of coarse version-keyed (SELECTED_SECTION_BAKE_CACHE_VERSION), so unchanged selected payloads don't re-render (~112 s in sampled run).

**P2 — Genericity debt**
1. Replace chunking.py KNOWN_DURATION_HINTS name table with contract-derived duration hints (movementType/complexity → duration proposal), keeping the generic 3–10 s fallback.
2. Retire exercise_name_suggests_lower_body_deterministic_review name-term fallback once contract coverage is confirmed from recent manifests; contract-derived exercise_requires_lower_body_motion becomes authoritative.
3. Document search aliases + jerk-variant regex as sanctioned exceptions (comments/docs only).

**P3 — Hardening**
1. Default per-exercise wall-clock budget / final-rejection cap for library runs (opt-out), replacing the unbounded-by-default max_final_output_rejections=0 / max_source_window_attempts=0 / timeouts=0.
2. Measure kinematicCutUsed rates across recent manifests; if kinematic-first dominates, shrink the grid-cut fallback budget (≤16 windows / ≤6 VLM candidates) accordingly.

**P4 — Maintainability (opportunistic)**: split bake_and_rank.py along existing seams only when already touching those areas.

**Verification approach** (per AGENTS.md principles): replay/benchmark harness + smallest sufficient `pytest -k` subset + `py_compile` for edited files; two-exercise test base for behavior; compare stage timings before/after; no validation weakening.

**Explicitly out of scope**: further VLM parallelism tuning, server keep-alive work, speculative decoding, validation threshold changes.