Switch the exercise movement generator's reconstruction backend to GVHMR (selectable; WHAM retained as default until validated). Minimal-mode profile deferred.

1. New `exercise_motion_pkg/gvhmr_runner.py` mirroring `wham_runner.py`'s interface and docker-job pattern: image `myworkoutassistant/gvhmr:torch2.3-cu121`, `--gpus all`, mounted workspace, container name `mwa-gvhmr-job-<uuid>`, static-cam `-s` flag (clips are YOLO-stabilized), GPU-lock integration equivalent to the WHAM docker lock. Input: stabilized inference segment mp4. Output: `hmr4d_results.pt` converted to the existing `motion.raw.json` schema (24 SMPL joints, frames, `source: "gvhmr"`, `coordinateSpace: "world_gravity_aligned"`) via GVHMR's own path (`make_smplx("supermotion")` + `smplx2smpl_sparse` + `smpl_neutral_J_regressor`), with Y-up handling recorded in metadata so the cleanup/world-alignment stage does not double-ground (pass-through for already-gravity-aligned inputs).
2. Backend selection: `motion_reconstruction_backend` setting (`wham` | `gvhmr`) threaded through `BakeAndRankRequest`/CLI/runner scripts (`-MotionReconstructor Gvhmr`; default `wham`). Route reconstruction calls where `wham_runner` is invoked; cache keys include the backend so WHAM/GVHMR artifacts never collide.
3. Verification: `python -m py_compile` on edited files; focused tests for the runner conversion (joint names/order/metadata); one real single-exercise run via `scripts/run_exercise_motion_generation.ps1 -MotionReconstructor Gvhmr` on the box jump source, checking the gates that previously failed (elevated terminal support, landing height) vs the WHAM artifact.

Out of scope: minimal mode, disabling any validation, library-wide default switch (stays `wham` until the box-jump/thruster batch validates GVHMR). Risk notes: GVHMR license research/non-profit only; preprocess ~30-60s/clip vs WHAM ~63s.